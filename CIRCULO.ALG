Algoritmo "circulo"

Var

   r : real
   area : real

Inicio

   escreval("Digite o valor do raio do circulo: ")
   leia(r)

   area <- Pi * Exp(r, 2)
   
   escreval
   escreval("O valor do raio do circulo: ", r)
   escreval("Area = ", area:8:3)

Fimalgoritmo